---
'seo-lint-next': minor
---

Add rules 11-25 covering language, viewport, client pages, hreflang, anchor text, 404/error boundaries, OG image dimensions, robots access to Next.js assets, breadcrumbs, next/head usage, SSR-disabled dynamic imports, next/font, redirect chains, and article dates.
